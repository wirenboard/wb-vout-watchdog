#!/usr/bin/env python3
# Module name is the hyphenated CLI command installed to /usr/bin, not an importable module,
# so it cannot be snake_case.
# pylint: disable=C0103
import sys

from wb_vout_watchdog.main import main

sys.exit(main(sys.argv))
